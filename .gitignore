.env.local

# секреты локального бэкенда — не коммитить (Фаза 8)
.env
# пароль Postgres прод-контейнера (docker-compose.prod.yml), только на сервере
.env.db

# Python-байткод и окружения — не коммитить
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.scholar_cache.json
scholar_slugs.txt
