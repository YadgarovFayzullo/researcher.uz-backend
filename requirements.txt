fastapi
uvicorn[standard]
sqlalchemy[asyncio]
asyncpg
pydantic-settings
psycopg2-binary
alembic
pgvector
python-dotenv
python-slugify
pyjwt
bcrypt
httpx
# MX-записи домена перед рассылкой (src/domain/email_check.py): ловит домены
# без почты вроде gmail.ru, не отправляя ни одного письма.
dnspython
boto3
python-multipart
slowapi
# Рендер первой страницы PDF в обложку издания (src/infrastructure/covers.py).
# pypdfium2 вместо PyMuPDF: Apache-2.0/BSD, без AGPL и без системного poppler.
pypdfium2
Pillow
# ИИ-проверка выпуска перед публикацией (src/domain/ai_review.py): сверка
# метаданных статьи с приложенным PDF через Claude API.
anthropic
# Чтение XLSX при импорте архивов (src/domain/importing.py). CSV читается
# стандартной библиотекой, но редакции присылают именно .xlsx.
openpyxl
